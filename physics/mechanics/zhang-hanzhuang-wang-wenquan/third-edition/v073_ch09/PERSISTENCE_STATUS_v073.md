# v073 持久化状态

## GitHub

目标仓库：`wzygxr/shuati`，分支 `master`。

目标目录：

`physics/mechanics/zhang-hanzhuang-wang-wenquan/third-edition/v073_ch09/`

持久化内容包括：

- README 与持久化状态；
- `PROGRESS_v073.json`；
- 第十章题位账 `CH10_SOURCE_INVENTORY_v073.csv`；
- 最新剩余总账与 QA 报告；
- 第九章完整 Markdown 的分片和重构脚本；
- SHA-256 清单。

累计 v073 Markdown 可以由 GitHub 中既有的 v072 累计源，加上 v073 状态页和第九章完整分片重构；PDF、原页图和 ZIP 等二进制文件以本轮对话附件为准。

## Google Drive

连接能够识别并创建项目目录，但此前实际文件写入曾明确返回：

- 目标位置不可写；或
- 用户 Drive 存储配额超限。

本轮不擅自删除任何旧文件，也不重复向同一失败目标空转上传。GitHub 作为当前稳定的文本版本通道。
