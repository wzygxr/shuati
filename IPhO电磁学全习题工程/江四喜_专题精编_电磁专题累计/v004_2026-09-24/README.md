# 江四喜《物理竞赛专题精编》电磁专题累计 v004

更新时间：2026-09-24

## 本版连续推进

- 专题 56《复杂电阻的分析与计算》：56-1--56-6，6/6；
- 专题 57《几种无限网络电阻的计算》：57-1--57-8，8/8；
- 本版新增 14 道正式题、28 道本质不同变式、14 道同类巩固，共 56 个训练单元；
- 累计专题 51--57：46 道正式题、92 道本质变式、46 道同类巩固，共 184 个训练单元；
- 下一断点：专题 58《非线性电路的分析与计算》58-1；专题 58 已冻结为 58-1--58-6。

## 主要纠错

1. 57-4：由 `R'=2R1R2/(R1+R2)` 得 `R'=(sqrt(2)-1)R0/2`，原印刷下一行的化简不一致。
2. 57-5：三行无限条带的中间根式常数应对应 `294+126sqrt(5)`，不是原印刷的 `395+126sqrt(5)`；最终 `R_AB=(4+sqrt(5)+sqrt(21))r/4`、`R_AC=(1+sqrt(5))r`。
3. 57-7：低频极限要求 `Z=[1+sqrt(1-4LCω²)]/(2jωC)` 的分支；原显示处的减号不满足首个串联电容的低频极限。

## 质量验收

- 累计 Markdown：10,282 行；
- 累计 PDF：195 页；
- 公式、图论与随机压力测试：278,295 项断言全部通过；
- 最大归一化残差：`7.460e-13`；
- PDF：Pandoc/XeLaTeX、`pdfinfo`、Ghostscript、字体嵌入、`startxref`、`%%EOF`、195/195 页渲染全部通过。

## GitHub 文本源码归档

受连接器和二进制文件大小限制，本目录保存可重建的文本源码包。执行：

```bash
bash reconstruct_source.sh
```

将生成：

```text
jiang_sixi_em_v004_text_source.tar.gz
```

并自动校验 SHA-256：

```text
933e04ac529c0d5009cc900af14be7a5352fbceba7b186d28a3e2c2bbc69bdf2
```

解压：

```bash
tar -xzf jiang_sixi_em_v004_text_source.tar.gz
```

归档包含累计 Markdown、专题 56/57 独立增量、复算程序与结果、质量报告、剩余总账、Manifest 和 PDF 预检摘要。完整 PDF、配图和 ZIP 以本轮 ChatGPT 会话交付文件为准；Google Drive 当前仍因存储配额已满而无法写入。
